{
  "name": "handle-email-signup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx functions-framework --target=handleEmailSignup --signature-type=http",
    "deploy": "npm run build && gcloud builds submit --config cloudbuild.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.17",
    "cloud-function-events": "^0.0.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@functional-vietnamese/cloud-function-events": "0.0.3",
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/pubsub": "^4.3.3"
  }
}

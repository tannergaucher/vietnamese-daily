{
  "steps": [
    {
      "name": "gcr.io/google.com/cloudsdktool/cloud-sdk",
      "args": [
        "gcloud",
        "functions",
        "deploy",
        "createDialog",
        "--gen2",
        "--region=us-east1",
        "--source=.",
        "--runtime=nodejs18",
        "--entry-point=createDialog",
        "--trigger-topic=create-dialog",
        "--env-vars-file=.env.yaml"
      ]
    }
  ],
  "options": {
    "logging": "CLOUD_LOGGING_ONLY"
  }
}
